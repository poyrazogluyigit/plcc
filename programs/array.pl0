const x = 1, array a = [1, 2, 3, 4, 5], array q = [3, 4, 5, 6];

var y, z;

array b;

function sum(arr, len);
var i, sum;
begin
    i := 0;
    sum := 0;
    while i < len do
    begin
        sum := sum + arr[i];
        i := i + 1;
    end;
    return sum;
end;

begin
    b := [1, 2, 3, 0, 0];
    y := 0;
    b[y] := b[0];
    y := b[2];
    y := a[3];
    while y < 5 do
        b[y] := b[y] + a[y];
    call sum(b, 5);
end.